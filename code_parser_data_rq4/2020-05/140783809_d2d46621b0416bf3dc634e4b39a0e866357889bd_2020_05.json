{
    "identifiers": [],
    "literals": [
        "string"
    ],
    "variables": [],
    "comments": [
        "Just used for labeling in logs",
        "If set, matches against the schedule name from CloudWatch events",
        "If set and length>0, only minutes matching this array will trigger 0-59",
        "If set and length>0, only hours matching this array will trigger (as defined by the timezone in cron config) 0-23",
        "If set and length>0, only days of week matching this array will trigger (as defined by the timezone in cron config) 0-6",
        "If set and length>0, only days of month matching this array will trigger (as defined by the timezone in cron config) 1-31",
        "If set and length>0, only months of year matching this array will trigger (as defined by the timezone in cron config) 1-12",
        "If set, the context must match this filter",
        "If set, the context must NOT match this filter",
        "If both match and nomatch are set, the noMatch dominates over match"
    ],
    "docstrings": [],
    "functions": [],
    "classes": []
}