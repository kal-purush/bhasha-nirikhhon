using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
Required Features
    Show the health of character in the game
    have a DealDamage(int damage) function to reduce the current health
    Update the current health to show in the game

Editable in editor
    Maximum Health
*/

public class Health : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}