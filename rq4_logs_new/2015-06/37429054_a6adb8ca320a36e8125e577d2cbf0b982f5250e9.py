import json
import clazz
import re
def as_statement_array(dct) :
	for d in dct:
		if hasattr(obj,'_child'):
			for o in obj._child:
				obj._child.append(default(o))
		return 



f=open('keys.dat',"r")
ff=f.read()
json.loads(ff,object_hook=as_statement_array)
