using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scr_MusicCalls : MonoBehaviour
{
    [SerializeField] SoundDefinition musicVar;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 1);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 2);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 3);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 4);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(musicVar.Sound, 5);
        }
        if (!=)
        {
            Scr_MusicManager.Instance.StopSound();
        }
        */
    }
}