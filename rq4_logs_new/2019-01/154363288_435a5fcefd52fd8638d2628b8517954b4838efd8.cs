using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scr_ToolSFX : MonoBehaviour
{
    [Header("Sounds")]
    [SerializeField] private SoundDefinition water;
    [SerializeField] private SoundDefinition extractor;
    [SerializeField] private SoundDefinition multiBuild;

    // Update is called once per frame
   /* void Update()
    {
        if ()
        {
                Scr_MusicManager.Instance.PlaySound(water.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(extractor.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(multiBuild.Sound, 0);
        }
    }
    */
}