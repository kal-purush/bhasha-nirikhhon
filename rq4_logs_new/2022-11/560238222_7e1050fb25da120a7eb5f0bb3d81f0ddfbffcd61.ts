export const queryBuilder = (
  orderBy: string,
  search: string,
  filter: string,
  cnt: number,
) => {
  if (search && orderBy && filter && cnt) {
  }

  if (search && orderBy && filter) {
  }

  if (search && orderBy && cnt) {
  }

  if (search && filter && cnt) {
  }

  if (search && orderBy) {
  }

  if (search && filter) {
  }

  if (search && cnt) {
  }
};